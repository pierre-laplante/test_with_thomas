{{
	function fact(n)
		/*
			@title factorial

			Compute a recursive factorial.

			@version 1.0 2018-04-18 laplante@sednove.com Initial version

		*/

		if (n<=0) then
			return 1;
		else
			return n*fact(n-1);
		endif
	endf

	fact(5);
}}
